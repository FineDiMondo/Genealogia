schedule:
  time: "08:00"
  timezone: "Europe/Rome"
  frequency: "daily"

merge:
  strategy: "ancestry_priority"
  conflict_handling: "ancestry_wins"
  deduplication: true
  source_attribution: true

notifications:
  enabled: true
  email: "daniel.giardina@gmail.com"
  smtp_host: "smtp.gmail.com"
  smtp_port: 465

git:
  auto_commit: true
  auto_push: false
  branch: "main"

error_handling:
  retry_attempts: 3
  retry_backoff_seconds: 60
  max_wait_hours: 24

