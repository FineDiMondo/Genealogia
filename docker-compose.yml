version: "3.8"

services:
  gedcom-sync:
    build: .
    image: gedcom-sync:latest
    container_name: gedcom-sync
    env_file:
      - .env
    volumes:
      - ./:/app
    restart: unless-stopped
    command: ["python", "gedcom_sync_orchestrator.py"]

